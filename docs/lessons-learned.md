# Lessons Learned

Append-only журнал уроков, которые субагенты накопили в процессе работы.

Формат строки: `| дата | агент | контекст | наблюдение | действие |`

| дата | агент | контекст | наблюдение | действие |
|------|-------|----------|------------|----------|
| 2026-05-01 | PM | Phase 3 (roadmap + backlog) | На момент написания roadmap'а 9 ADR (001-009) уже приняты, но конкретного выбора embedding-модели нет (ADR-009 — decision frame, не decision). Это означает: первая Dev-итерация технически не может стартовать «вживую» — только на mock `EmbeddingProvider` (DEV-019). Critical path для запуска кода фактически проходит через OWNER-002 (`yc` CLI) → RES-002-closeout → SA-001 (concrete model ADR), а не через ADR-001..009. | В backlog'е выделил DEV-019 (mock-mode) как разблокировщик для DEV-001..014, чтобы core/ports/тесты можно было катать без YC. Реальный smoke на YC FM — только после OWNER-002 + DEV-020/021. |
| 2026-05-01 | PM | Phase 3 | В принципиальной архитектуре §10 номера ADR (010-014) — это «рабочие имена» SA, не реальные файлы. ADR в `content/00-project/adr/` имеют свою нумерацию (001-009 написаны). Чтобы избежать конфликта нумерации при создании новых ADR — назначил им номера 010-019 в backlog'е (SA-001..010), последовательно после уже существующих 001-009. | Когда SA будет писать новые ADR — продолжить нумерацию с 010, не привязываясь к §10-таблице принципиальной архитектуры. После приёма обновлять §10 cross-link'ами. |
| 2026-05-01 | PM | Phase 3 | Backlog существующего файла `content/00-project/backlog.md` содержал устаревший stub BL-001 (выбор JVM-библиотеки для pgvector — JDBC, pgvector-java, Hibernate Vector, Spring AI). После принятия ADR-002 (SMP-only data access) этот выбор отменён архитектурно: JDBC запрещён в любом виде, любые `org.postgresql.*` зависимости в JAR — критическая ошибка ([JdbcImportBanSpec](DEV-004)). | BL-001 удалён при перезаписи backlog.md. Если возникнет соблазн вернуться к JVM-pgvector-обёрткам — сначала пересмотр ADR-002 + явный sign-off owner'а на снятие red-line «БД защищена, прямого доступа нет физически». |
| 2026-05-01 | PM | OWNER-001 | Канал доступа к БД из JAR — Hibernate `SessionFactory` через Spring `beanFactory.getBean('sessionFactory')`, не `api.db.query` SMP. Уточнено owner'ом по итогам анализа baseline-скрипта `Вспомогательный модуль для векторизации.groovy`. Ранее в CLAUDE.md и ADR-002 предполагалось «всё через SMP API» — это касается script-модулей; для JAR работает другой канал. | Создан `HibernateSessionProvider` в `adapters/db/` по образцу `SmpSuperUserRunner` из эталонного `naumen-smp-mcp`. Апдейт CLAUDE.md (раздел «Доступ к окружению» + красные линии). Полный апдейт ADR-002 — задача SA-011. Memory: `reference_smp_db_access_hibernate.md`. |
| 2026-05-01 | PM | OWNER-001 | `@InjectApi` в JAR-классах компилируется, но runtime-поля остаются null. Известное ограничение SMP-платформы (см. эталонный `naumen-smp-mcp` memory `reference_inject_api_jar_limitation.md`). Напрямую `beanFactory.getBean(...)` из JAR-класса тоже не работает — нет binding scope'а. | Паттерн **script-as-binding-carrier**: SMP script-module (`public/*.groovy`) держит `api`+`beanFactory`+`modules` и передаёт их в JAR-классы через конструктор Runner-обёрток (`HibernateSessionProvider`, `SmpSuperUserRunner` и т.п.). Зафиксировано в красных линиях CLAUDE.md и в SA-011. |
| 2026-05-01 | PM | pm-review | 10 ADR имели статус `Proposed`, не входящий в enum `.doc-root.yaml` (`Draft / Review / Approved / Superseded`). owner-001-tracking-doc — `Closed`, тоже вне enum'а. | Массовая замена `Proposed → Draft` в frontmatter и теле всех ADR; `Closed → Approved` в owner-001. На будущее — добавить в `/pm-review` чек: статусы content-файлов ∈ enum из `.doc-root.yaml` (запустить как pre-commit hook или часть skill'а). |
| 2026-05-01 | PM | pm-review | Внутренние контакты команды Naumen (email `*@naumen.ru`, Telegram-handles) в `content/00-project/stakeholders.md` и `owner-questions/` формально подпадали под красную линию «не включать PII». | Owner подтвердил: Gramax-public публикуется внутри корпоративного периметра Naumen, наружу не уходит → внутренние контакты команды допустимы для PoC. Уточнено в CLAUDE.md (красные линии) и в memory `project_pii_scope.md`. PII клиентов/заявителей по-прежнему запрещён. |
