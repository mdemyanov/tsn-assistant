# Lessons Learned

Append-only журнал уроков, которые субагенты накопили в процессе работы.

Формат строки: `| дата | агент | контекст | наблюдение | действие |`

| дата | агент | контекст | наблюдение | действие |
|------|-------|----------|------------|----------|
| 2026-05-06 | PM | Шаблон / Gramax-структура | `README.md` в подпапках `content/` Gramax не индексирует (раздел невидим в навигации); плоская frontmatter-нотация рендерится непредсказуемо | Перевели всё на `_index.md` (без `properties:`), добавили `scripts/validate-content.py` (7 проверок) и шпаргалку в CLAUDE.md. Источник — `pg_vector_service/docs/gramax-skills-update.md`. |
| 2026-05-07 | PM | Wave 2 / multi-template | Шаблон поддерживал один тип проекта; пользователи имели разные шейпы (delivery, KB, methodology, course) и не могли использовать общий шаблон | Profile-driven система с 7 профилями (project + kb-team baseline; 5 stub'ов в задел Wave 3+); расширенный subagent-каталог (10 ролей; AT+Tester объединены через --mode); 3 pipeline'а (project-planning, BA-acceptance, critical-path); apply-overlay.sh с ops add/replace/delete и strict delete-non-empty. SDD-режим: 44 задачи в 8 фазах, ~44 атомарных коммита; финальный smoke + validate-profile.py 0/0; backwards-compat (legacy init без --profile → fallback на project) сохранён. |
| 2026-05-07 | PM | Wave 2 — content/ portability | Wave 2 dev meta (research file) лежал в content/, что ломало kb-team smoke (project-specific Тип контента) | Relocated `multi-template-landscape.md` → `docs/research/`; убрали `Фаза` из glossary.md как необязательное property для cross-profile portability. Урок: development meta для template-проекта должно жить в `docs/`, а `content/` — только baseline scaffold + примеры применимые ко всем профилям. |
| 2026-05-07 | PM | Wave 2 — on_value deferred | Plan §T40 promised init_prompts → on_value → in-memory manifest mutations (e.g. compliance opt-in via 152-fz answer flips subagents.compliance: core). Реализация не была подключена end-to-end (init.sh собирал ответы, apply-overlay.sh их не читал) | Removed init_prompts из project manifest и docs (commands/init.md) для Wave 2; on_value перенесён в Wave 3 task. Урок: on_value мутации не влияют на filesystem state (subagent статусы — declarative metadata в AGENTS.md matrix, не runtime-enforced), поэтому deferral безопасен; но обещание в plan'е без impl — debt, который надо или закрывать, или явно не обещать. |
| 2026-05-07 | PM | Wave 3 / tech-debt closure | Wave 2 финальный review нашёл 2 Critical + 9 Important + 7 Minor; Critical устранены в W2-T44b, остальное — debt | Wave 3 SDD: A1 (on_value e2e через INIT_PROMPT_<id> env vars + dotted-path mutation in-memory) + A2 (bash→Python helper `_apply_profile.py`, ×N×4 subprocess shells → 1 helper invoke + JSON ops plan) + A3-A7 small fixes + F4 (`scripts/check.sh --fast/--full`) + F3 (git pre-commit hook `.githooks/pre-commit` + `install-hooks.sh`) + G1/G2/G3 (architecture-overview, troubleshooting, examples × 2). 23 задачи в 6 фаз; subagent-driven с двухстадийным review. Финальный smoke `check.sh --full`: 135 ассертов (validate-content + validate-profile + 26+30+79). Уроки: (1) bash subprocess-loops на структурированном YAML — antipattern на bash 3.2 (нет ассоц.массивов); Python helper + JSON plan делает logic тестируемой и устраняет quoting hazards. (2) IFS=$'\t' между bash и subprocess collapse'ит empty fields — для TSV с ОПУСТЕВАЮЩИМИ полями использовать ASCII Unit Separator `\x1f`. (3) Subagent-driven с code review между задачами ловит latent footguns (Path("") = Path("."), bash expand на enum `\|` в grep — `grep -qF` solve'ит, hyphenated prompt ids, etc.) — review надо на каждой phase, не только финал. |
| 2026-05-07 | PM | Wave 4a / override mechanic + kb-product pilot | После Wave 3 шаблон имел 2 stable профиля (project, kb-team) и 5 stub'ов; базовые промты агентов были generic, без per-profile customization | Wave 4a SDD: P0 (W3-minors closure: ManifestError exception + ops_count optimize + cleanup) + P1-P3 overrides core (RES-410 research 7 систем + Jinja2/Helm/Kustomize, BA-411 BRQ override mechanic + BA-412 kb-product BRQ, SA-420 ADR-001 base+delta merge + SA-421 spec _resolve_agents.py + SA-422 kb-product spec, DEV-430..434: `scripts/_resolve_agents.py` 243 lines + 14 unit-tests merge_delta + `_apply_profile.py` emit resolve_agents step + `apply-overlay.sh` op_resolve_agents handler + `validate-profile.py` M11 5 sub-checks + 10 ассертов + demo override tech-writer на kb-team) + P4 kb-product pilot (manifest stable + content-scaffold 4 разделов + agent-overrides/tech-writer.md customer-facing + examples/kb-product-example/ + 13 fine-grained T-W4a-P4-* ассертов) + P6 docs (architecture-overview Agent overrides раздел + mermaid + troubleshooting 5 FAQ override edge-cases). 21 задача в 6 фаз. Финальный smoke: ~96+ ассертов зелёные. Уроки: (1) RES-410 артефакт ПЕРЕД SDD ловит improvements до того как они становятся expensive: `{{super}}` placeholder (Jinja2-аналог) добавлен в spec через §14 refinements after research, дёшево в W4a, дорого retrofit'ить позже. (2) Standalone Python helper (`_resolve_agents.py` отдельно от `_apply_profile.py`) + pure `merge_delta` (no IO beyond reads, no marker prepended) — separation of concerns; helper тестируется как unit (14 tests) без mocking apply-overlay flow. (3) Demo override на stable профиле (kb-team) во время Phase 3 — proof-of-concept до создания нового профиля; ловит integration bugs до того как pilot профиль (kb-product) вошёл в работу. (4) Profile manifest delete operations должны cover ALL non-applicable folders из base content/ — kb-product изначально missed `content/10-domain/` (T17 review caught), вызвав broken validate-content в init'd проекте. Урок: при создании нового профиля — `diff` baseline content/ tree против profile-specific content-scaffold/ для exhaustive deletes. (5) Bash `set -u` + empty array требует `${arr[@]+"${arr[@]}"}` safe-expansion pattern — поймано regression suite (T-OP-DELETE-STRICT) при переходе с `$init_flag` (string) на `init_args=()` (array) в W4a-T2. (6) Override frontmatter validation: `extends:` must equal file basename без `.md` — `override_path.stem` check предотвращает rename-induced silent breakage. (7) IO boundary error handling: каждая `yaml.safe_load` обернута в try/except → clean stderr message, не stack trace (T10 review caught). (8) Subagent-driven sequential review между задачами: spec compliance первой, потом code quality — ловит different classes issues; T8 spec был internally inconsistent (override_path unbound + missing extends mismatch check), code quality pass поймал и обновил spec до того как DEV-430 закодировал. |
| 2026-05-07 | PM | Wave 4c-B / init.sh UX customization | После W4c-A 7/7 stable профилей init.sh показывал плоский список имён без описаний; пользователь не имел post-selection summary и confirm gate; не мог отменить применение профиля до destructive операций (.git wipe + apply-overlay) | Wave 4c-B SDD sequential (small scope): BA-50 (BRQ — 3 FR + NFR + 3 open questions для SA) → SA-60 (spec — 3 функции print_profile_menu/print_profile_summary/confirm_apply + 14 ассертов; SA-60 потребовал retry из-за API socket drop) → DEV-70+71 (combined: 3 функции в init.sh + 14 T-W4c-B-* ассертов в test-template.sh; passed 132 → 146) → DEV-72 (extending.md description guidance + CLAUDE.md команды раздел) → DEV-73 (final check.sh --full: 227 passed, 0 failed; W4c-A 213 → +14). 7 atomic коммитов. Уроки: (1) **`init.sh` — destructive operation, НЕ запускать в worktree без `mktemp -d` изоляции:** PM случайно запустил `printf 'test\n...' \| bash scripts/init.sh` напрямую в worktree для демо menu — init.sh wipe'нул `.git` файл (worktree gitfile pointer) и создал brand new git repo поверх. Recovery через `git worktree remove --force` + `git worktree prune` + `git worktree add .worktrees/w4c-b epic-w4c-b` восстановил из branch refs. Урок: для verify init.sh всегда `TMP=$(mktemp -d) && rsync -a --exclude=.git ...` — НИКОГДА в worktree. (2) **API socket disconnects во время Agent dispatch:** SA-60 первая попытка прервалась `API Error: socket connection closed`; артефакт не создан, коммит отсутствует. Retry с тем же prompt'ом через ту же subagent_type сработал. Урок: при API disconnect — verify file existence/git log; retry без paranoia (idempotent prompts). (3) **Объединение DEV-70+71 в один dispatch экономит latency:** scope маленький (3 функции + 14 ассертов на disjoint files); single subagent делает обе задачи и серьёзно выигрывает по времени vs sequential dispatch. Worked because: (a) tests directly exercise the implementation, no integration risk; (b) commit boundary clear (init.sh commit + test-template.sh commit). (4) **Confirm-gate cancel test (FS не тронут) при non-TTY невозможно:** AC-3.4 BRQ требует "n → exit 0, FS unchanged"; но pipe stdin non-TTY → bypass triggered → confirm НЕ показывается. Полный AC требует PTY emulator. Dev пропустил полный assertion с TODO commentary; subset coverage (force bypass + no-TTY bypass) acceptable. Урок: BRQ AC должны учитывать testability — pipe stdin тестирует ответ "n" но не "FS не тронут" (бо confirm не показалось). (5) **Manifest field `audience` теперь semantic UX requirement, не просто metadata:** до W4c-B audience был optional metadata; теперь init.sh menu показывает `[для: <audience>]` если поле есть. 4 W4c-A профиля имеют audience (product/methodology/course есть; custom — намеренно нет, по anti-opinion); добавили в extending.md явное руководство для авторов будущих профилей. |
| 2026-05-07 | PM | Wave 4c-A / 4 stub профиля → stable | После W4b шаблон имел 3 stable профиля (project, kb-team, kb-product) и 4 stub'а (product, methodology, course, custom) с пустыми manifest'ами без scaffold/operations/overrides; пользователи не могли применять `--profile <name>` для этих 4 типов | Wave 4c-A SDD parallel: Phase 0 RES-01 (research user shapes 4 профилей: GitHub spec-kit, GitLab Handbook, Coursera, Obsidian vault — 234-строчный артефакт с бенчмарками + properties + override candidates) → Phase 1 BAs (4 параллельных BRQ) → Phase 2 SAs (4 параллельных spec) → Phase 3 Devs (4 параллельных profile impl + example regen) → DEV-FIX (qa special-case в `_resolve_agents.py`) → DEV-34 (T-W4c-* assertions) → DEV-40 (architecture-overview + CLAUDE.md). Финальный smoke `check.sh --full`: **213 ассертов зелёные** (W4b 189 → +24). 17 atomic коммитов. Уроки: (1) **Параллельные subagents в одном worktree без явного `git add <specific-file>` создают race-condition коммиты:** BA-12+13 их BRQs упаковались в один commit под именем BA-12; DEV-31 случайно подцепил untracked custom-example. Урок: в prompt для parallel agents — обязательно `git add <explicit-paths>` (не `git add -A` / `git add .`), commit'ы должны быть atomic per-task. (2) **Pre-existing bug latent в инфраструктуре проявляется только когда новый профиль активирует cold path:** product (qa: core + non-empty agent_overrides) триггерит `_resolve_agents.py:218` искать `qa-agent.md`, которого не существует (роль `qa` имеет 2 файла: qa-author + qa-runner). Урок: каждое новое profile addition нужно проверять на init.sh end-to-end до считать done — DEV-30 поймал, fix через special-case в resolver'е (`if role == "qa"` → emit обоих qa-author и qa-runner). (3) **Research-фаза экономит BA/SA время:** RES-01 234 строки с бенчмарками и open questions дал BA-{10..13} прямые входные данные; SA-{20..23} получили готовые scaffold candidates; total BA+SA latency ≈ 5-7 минут параллельно vs 20+ если бы каждый делал свой research. Открытые вопросы из research (granularity иерархии methodology, course numbering convention) BA решали с явным "рекомендация ... обоснуй". (4) **Anti-opinion профиль (custom) — особый case:** SA-23 + DEV-33 явно НЕ добавили overrides, числовые префиксы, opinionated workflow. Anti-pattern checks в test-template.sh (T-W4c-CUSTOM-no-numbered, no-overrides) защищают принцип от случайного нарушения. (5) **Pipeline-orchestrator skill (`/pipelines/project-planning`) не подходит для multi-feature эпика:** ожидает короткий slug, на длинный prompt падает с substitution glitches. Для эпиков такого размера лучше PM-orchestrator + parallel subagent dispatch напрямую (как W4a/W4b/W4c-A). (6) **Bash сессия `cd` в subagent не сохраняется в parent context:** PM в main-context может оказаться в `/Users/.../project_template` или в worktree между Bash-вызовами; `git status`/`git log` без явного `cd <abs path>` показывают разное состояние. Урок: для критичных проверок (verify SA/Dev artefacts) — всегда `cd /abs/path/.worktrees/...` или абсолютные пути в командах. (7) **Cross-cutting roles иногда требуют `*-agent.md` множественных файлов:** qa имеет 2 mode'а; роль в manifest = `qa`, файлов = 2. Нужна архитектурная convention: либо single-file с `--mode` parameter (текущий subagent invocation pattern), либо split в manifest на `qa-author`+`qa-runner`. Принят compromise: single role в manifest + special-case resolver. Долгосрочно — считать в W4c-B/C при рефакторинге role schema. |
| 2026-05-14 | PM | Эпик uv-enforce (pre-W5 production hardening) | Шаблон требовал глобальный `pip install pyyaml`; init.sh содержал ~10 inline `python3 -c` блоков; первый запуск падал с непрозрачным ImportError при отсутствии PyYAML на хосте; воспроизводимость зависела от состояния Python-окружения | Эпик SDD pipeline RES → BA → SA → Dev (RED→GREEN) → DevSecOps → PM-INTEG: RES-001 (research uv ecosystem + PEP 723 vs alternatives) → BA-010 (BRQ — 7 FR + 9 AC + NFR) → SA-020 (spec + ADR-002 — Option A PEP 723, fallback на python3 запрещён, INIT_FORCE НЕ bypass'ит prereq) → DEV-031..035 (PEP 723 headers в 6 .py + новый `_init_helpers.py` с argparse subcommands + `check_prerequisites()` в init.sh + uv-guard в check.sh/test-*.sh + 10 ассертов T-UV-PREREQ-* + docs Prerequisites section) → DevSecOps SEC-040 (ADR-003 supply-chain stance: uv.lock не коммитим, pyyaml>=6.0,<7.0 range адекватен, SBOM/CI — downstream-only) → PM-INTEG (relocation content/→docs/ + lessons). 12 atomic коммитов. Финальный smoke `check.sh --full`: **157 ассертов в test-template.sh, 0 failed; ~30+ в validate-content/profile/etc.** Уроки: (1) **Артефакты эпика про сам шаблон должны жить в `docs/`, не в `content/`.** BA/SA сначала положили BRQ/spec/ADR в `content/{30-requirements,40-architecture,00-project/adr}/` — это сломало T-W4b-BASELINE (baseline content/ = 2 файла) и T-INIT-PROFILE-KB/T-W4a-P4-noise (overlay не должен бороться с самодельными разделами при apply). PM-INTEG-1 перенёс 4 файла в `docs/{requirements,architecture,adr,implementation}/` + удалил 5 stub `_index.md`. Урок: **при работе над шаблоном** артефакты SDD = `docs/`; `content/` — только baseline scaffold + примеры применимые ко всем профилям. Это паттерн уже встречался в W2 (multi-template-landscape.md) и W4b (T2-fix перенос framework docs) — формализован: добавить в чек-лист BA/SA prompt'ов «при работе над шаблоном артефакты в docs/». (2) **`init.sh` — destructive (wipe .git), prereq-gate должен сработать ДО любых mutations.** `check_prerequisites()` располагается сразу после `set -euo pipefail`, до парсинга args, до меню профилей. T-UV-PREREQ-04 защищает: при отсутствии uv в PATH `.git` остаётся целым (никаких destructive ops). `INIT_FORCE=1` намеренно НЕ bypass'ит — safety > automation convenience (зафиксировано в ADR-002). (3) **PEP 723 inline metadata + `uv run` — minimal-overhead enforcement без pyproject.toml/lock-файла.** Каждый `.py` self-describes deps; `uv run script.py` авто-resolve'ит в эфемерное venv; ноль ручного venv management. Single-dep случай (PyYAML) — не требует `uv.lock` (PEP 723 + range pin = достаточная воспроизводимость). Решение пересмотреть при ≥2 deps или регуляторном требовании на pinned hashes (ADR-003 фиксирует 5 триггеров). (4) **Subcommand-dispatch helper (`_init_helpers.py` + argparse) > inline `python3 -c`.** До эпика init.sh содержал ~10 многострочных `python3 -c "..."` блоков с heredoc'ами, которые: (a) не тестируемы изолированно, (b) ломаются при escape edge-cases (одинарные/двойные кавычки в YAML), (c) дублируют логику YAML parsing в bash. Замена на `uv run scripts/_init_helpers.py <subcommand> [args]` дала: тестируемость, type-checking возможность, единое место PEP 723 deps, читаемость. Урок: любой повторяющийся `python3 -c` ≥2 строк — кандидат на helper script с argparse. (5) **DEV-035-RED first (failing tests до impl).** 10 ассертов T-UV-PREREQ-* написаны RED-коммитом до DEV-031..034 — TDD-подход поймал 3 test-robustness regressions (T-UV-PREREQ-02/05/07 под `set -euo pipefail`), потребовавших отдельного fix-коммита (9f2d5c6). Урок: при `set -euo pipefail` тестах — `set +e` обёртка обязательна вокруг любого command, чей exit-code проверяется ассертом. (6) **DevSecOps как embedded review, не security-theater.** SEC-040 не нашёл Critical/High/Medium findings, но дал follow-up календарь: review pin'а `<7.0` через 6 месяцев + supply-chain секция в extending.md (опционально). Audit подтвердил: PyYAML — high-trust пакет (15+ лет, 2FA enforced PyPI'ем, `yaml.safe_load` используется правильно). Reference-memory `reference_uv_enforcement.md` содержит resolved Q1-Q5 для downstream проектов. (7) **Owner-разрешение Opus для subagent'ов в этом репо** (memory `feedback_subagent_opus_authorized`) применено к Dev и DevSecOps; качество > бюджет в SDD-фазе. Sonnet остаётся default для parallel parallel batches. |
| 2026-05-07 | PM | Wave 4b / baseline minimization + W4a Medium cleanup | W4a оставил `content/` baseline дублирующимся с `docs/overlays/profiles/project/content-scaffold/` (00-project, 10-domain, 30-requirements, 40-architecture, 60-implementation, 70-operations с _index.md), kb-team и kb-product вынуждены делать 5-6 op:delete; manifest schema имела dead-field `role_status`; `_apply_profile.py` имел inconsistent error handling | Wave 4b SDD: P1 baseline migration (T1 audit + sync research/_index.md + adr/_index.md в scaffold; T2 git rm 6 baseline папок; T2-fix перенос 6 W4a framework docs из content/ в docs/{adr,architecture,requirements,glossary.md}/; T3+T4 drop 5+6 op:delete из kb-team/kb-product) + P2 examples regen (T5/T6/T7 параллельно; T8+T8-fix перенос T4 scaffold checks с pre-init на post-init; T9 verify validate-content + fix _validate_common placeholder substitution) + P3 W4a cleanup parallel (T10 unified ProfileError + 2 negative tests + ManifestError alias; T11 ops_count via grep -c -v вместо python3; T12 explicit multiline reason TSV-strip assertion; T13 drop role_status из 2 manifests + 6 fixtures; T13-fix `_resolve_agents.py` defensive isinstance guard для null override; T14 verify-only signature) + P4 (T15 full smoke + T16 lessons). 16 atomic + 6 fix-commits = 22 коммита. Финальный smoke `check.sh --full`: 189 ассертов зелёные (W4a 176 → +13). Уроки: (1) Spec-by-memory может содержать stale задачи — reality-check после spec и до plan экономит subagent time: 3 verify-only из 16 (T9, T12, T14) обнаружили что код уже refined в финале W4a; план явно отметил их verify-only с `git commit --allow-empty` для трассировки. (2) Audit boundary: T1 проверял только `_index.md` файлы и не поймал реальные .md документы (ADR-001, glossary, specs, requirements) → T2 удалил их collateral damage; владельческая эскалация поймала класс side-effect, T2-fix перенос в docs/ как framework-internal. Урок: при baseline-minimization audit должен проверять `git ls-files content/` (все files), не только filename pattern. (3) Backwards-compat alias (`ManifestError = ProfileError`) — дешёвый refactor с нулевым риском для внешних callers. (4) Pre-existing bug exposed by fixture cleanup: T13 sed удалил единственный sub-key из `agent_overrides.active:` fixture → exposed AttributeError в `_resolve_agents.py:227`. Не fixed in fixture (был бы paper-over) — fixed в resolver через `isinstance(override_entry, dict)` guard, защищает реальный manifest с малформированным null override. (5) Test ordering matters after baseline change: T8 добавил T-W4b-BASELINE pre-init asserts, но T4 (scaffold subdir checks) тоже был pre-init — после W4b baseline пуст, T4 fail; T8-fix перенёс T4 после T5 (init.sh). Урок: при удалении baseline content проверять test-template.sh ассерты на pre/post-init split. (6) `_validate_common.py` placeholder substitution с кавычками (`"PLACEHOLDER"`) ломает YAML scalars с trailing текстом (`title: "PLACEHOLDER" — rest`) — T9 subagent поймал и зафиксил inline (verify-only задача → real fix-commit). Substitution в YAML должен быть plain без кавычек. (7) Examples регенерация диффом проверяет идемпотентность: T5/T6/T7 показали корректное revert от example-specific values к template placeholders; T7 показал что resolver теперь генерирует все non-disabled subagents (4 новых файла) vs только tech-writer ранее. (8) Subagent-driven parallel batches с disjoint files: T1+T10+T11+T13 в parallel (4 implementations за один dispatch round), no merge conflicts; review cycle handled per-task sequentially. Скорость заметно выше чем sequential — owner-instruction overrides default skill rule о sequential implementations когда files реально disjoint. |
| 2026-05-14 | PM | Эпик spdd-integration (Wave 5) | Шаблон уже на ~70% реализовывал идеи SPDD (Thoughtworks, апрель 2026) как процесс, но без явных правил: расхождение upstream/downstream чинилось вручную, инварианты артефактов размазаны по NFR, drift между слоями не ловился автоматически | Эпик SDD pipeline RES → BA → SA → QA-author → Dev → QA-runner → OPS → BA-acceptance → /pm-review: RES-001 (key principles из 2 источников, выписал что `drift_pairs` — наше расширение SPDD, не оригинал) → BA-001/002/003 (3 требования с dogfooding Safeguards-секцией) → SA-001 (ADR-004 + design-spec; profile-marker = `profile:` в `.doc-root.yaml`, glob через `pathlib`, bypass = trailer `skip-drift: <reason>`) → QA-001 (4 failing stubs, 22/23 AC покрыто, 1 обоснованный пропуск — e2e LLM-smoke) → DEV-001..005 (CLAUDE.md two-way sync + README pointer + AGENTS.md bullet, Safeguards в ba/tech-writer agents, drift_pairs в 7 manifests, `_drift_check.py` модуль + pm-review integration, schema validation) → DEV-extras (upgrading-from-template для pre-SPDD проектов) → QA-runner (1 regression T-UV-PREREQ-06 + 2 contamination от content/-артефактов) → DEV-007/008 fix (динамический подсчёт PEP 723 файлов + relocate content/→docs/ через git mv) → OPS-001 (49 SPDD smoke checks по 7 профилям, T-SPDD section) → BA-acceptance verdict PASS. 11 atomic коммитов на epic-ветке, merge --no-ff в private (40 файлов, 3595 строк). Финальный smoke: SPDD stubs 68/68 green, test-template.sh 206/206 green, check.sh GREEN, validate-content/profile 0 errors. Уроки: (1) **Multi-session continuation эпика — git log + memory check ДО dispatch.** Запуск orchestrating-сессии после прерванной — DEV-001..004 и OPS-001 уже были committed в предыдущей сессии; новая сессия делегировала тем же subagent'ам, агенты обнаружили committed state и не дублировали, лишь верифицировали. Урок: PM при возобновлении эпика — ВСЕГДА `git log --oneline epic/<slug> ^private` + чтение MEMORY.md перед первой Agent-диспетчей; иначе subagent'ы делают идемпотентную noop-работу. (2) **Артефакты эпика про шаблон → `docs/`, не `content/` — повторение урока uv-enforce.** PREP-commit fa24920 положил RES/BA/SA/QA артефакты в `content/{10-domain,30-requirements,00-project/adr}/`, что сломало T-W4b-BASELINE (baseline content/ должен быть = {`_index.md`, `.doc-root.yaml`}). Relocate через `git mv` в `docs/{research,requirements,adr}/` + удаление 5 stub `_index.md` + обновление 4 cross-references. Урок (формализован 3й раз — W2, W4b, uv-enforce, spdd-integration): **в шаблонных BA/SA/QA prompt'ах явная инструкция «при работе над шаблоном артефакты в `docs/`, content/ — только baseline scaffold».** Кандидат на добавление в base-промт BA/SA-агентов или в CLAUDE.md «правила работы над шаблоном». (3) **Hardcoded counts в тестах — антипаттерн; используй dynamic discovery.** T-UV-PREREQ-06 проверял `грep -rl '# /// script' scripts/ | wc -l == 6`; DEV-005 добавил `_drift_check.py` (7-й PEP 723 файл) → red. Fix: `UV06_TOTAL=$(grep -rl '# /// script' scripts/ | wc -l); UV06_COUNT=$(...); [[ $UV06_TOTAL -eq $UV06_COUNT ]]` — самосогласованный invariant. Урок: **любой test assertion с literal count — кандидат на dynamic discovery.** Альтернатива (если count важен сам по себе) — отдельная константа + тест на её обновление при добавлении файла. (4) **`drift_pairs` в manifest — наше расширение SPDD, не оригинал.** RES-001 явно зафиксировал: в Thoughtworks SPDD логика расхождений живёт в tooling (openspdd), автоматизация — future direction. ADR-004 § Alternatives задокументировал это как осознанное extension под template-инфраструктуру. Урок: **при заимствовании паттернов из внешних методологий ALWAYS отделять «что есть в источнике» от «как мы адаптировали» в ADR.** Это помогает downstream-проектам понять, где наша уверенность высокая (мы это построили), а где — гипотеза (SPDD это рекомендует, но мы интерпретировали так). (5) **Dogfooding-петля как natural test.** Каждое из 3 BA-требований (BA-001/002/003) ОБЯЗАНО содержать секцию «Инварианты и Safeguards» — это и есть предмет BA-002. Если шаблон Safeguards-секции ломан или семантика непонятна BA, не получится написать саму статью BA-002. Это бесплатный integration-test на уровне требований. Урок: **для documentation-side фичей dogfooding-петля = AC.** Применимо к любой meta-фиче шаблона (manifest schema, agent prompt format, ADR-структура). (6) **Safeguards ≠ AC — семантическое разграничение требует явного комментария.** Safeguards = hard constraints, бинарные («modelId not null», «у роли есть backup-owner»); AC = ожидаемое поведение, описательные. Без явного комментария в шаблоне будущие BA/tech-writer писали бы туда обычные AC и Safeguards-фича превратилась бы в дубль existing AC-секции. SA-decision: SA-NOTE-комментарий в шаблоне обоих агентов (ba-agent.md + tech-writer-agent.md). Урок: **новое поле, семантически близкое к существующему = обязательный inline-комментарий о различии**, иначе drift через 3-5 итераций. (7) **Backward-compat через graceful degradation, не feature-flag.** `_drift_check.py` при `drift_pairs=None` или `drift_pairs=[]` → INFO-skip без FAIL; `validate-profile.py` обрабатывает manifest без поля `drift_pairs` как валидный. Pre-SPDD проекты не ломаются. Урок: **для process-rule изменений в шаблоне — graceful degradation (отсутствие поля = старое поведение); feature-flag не нужен, потому что миграция — opt-in через добавление поля.** Opcjonal `docs/upgrading-from-template.md` объясняет downstream, как включить. (8) **`/pm-review` skill корректно показал чистоту merge:** 0 errors в обоих validator'ах, 40 файлов diff, нет orphan worktree'ев, lessons-learned пополнен этой записью. Урок: **/pm-review checklist работает; не пытаться merge'ить без неё** — даже когда BA-acceptance уже PASS, /pm-review ловит вещи уровнем выше (целостность content/, profile manifest schema, hanging worktree'и). |
