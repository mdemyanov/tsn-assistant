profile: custom
title: "{{PROJECT_NAME}}"
properties:
  - name: Тип контента
    type: enum
    values: [Заметка, Решение, Справка]
    required: true
  - name: Статус
    type: enum
    values: [Draft, Review, Approved, Archived]
    required: true
