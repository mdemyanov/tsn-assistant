# Wave 4c-B Implementation Plan — Init UX Customization

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development.

**Goal:** Улучшить UX `scripts/init.sh` при выборе профиля: меню с описаниями вместо плоского списка имён + post-selection summary + confirm gate перед применением.

**Architecture:** Расширения только `scripts/init.sh` (без изменения `_apply_profile.py` / `_resolve_agents.py` / manifest schema). Используем существующее поле `description` в каждом manifest.yaml. Добавляем новое поле `audience` (опц., уже есть в kb-team/kb-product/product) для дополнительной справки в меню.

**Tech Stack:** Bash (init.sh), Python 3 (для парсинга YAML — уже используется), test-template.sh для assertions.

**Spec:** `docs/architecture/spec-init-ux.md` (TBD — пишется SA в Phase 2)

## State

**Status:** active
**Current phase:** Phase 1 (BA)
**Started:** 2026-05-07
**Worktree:** `.worktrees/w4c-b` (branch: `epic-w4c-b`, base: `private @ 8bee3f7`)
**Blockers:** —

## Scope (narrow)

**В скоупе:**
- (1) Меню профилей с **описанием** (manifest.description) и **аудиторией** (manifest.audience, если есть) — вместо плоского `- name`.
- (2) После выбора профиля — **summary block**: что будет применено (# operations, # overrides, # subagents core/optional/disabled).
- (3) **Confirm gate** перед применением: "Apply this profile? (Y/n)" — если non-interactive (no TTY) или `INIT_FORCE=1`, skip prompt и proceed.
- (4) Обновить документацию в CLAUDE.md и extending.md.

**Не в скоупе (отложено в W4c-C):**
- Override toggles (per-override y/n prompts) — требует расширения `init_prompts` mechanic
- Profile recommendation wizard ("what are you building?")
- Migration tooling (`migrate-profile.sh`, `upgrade-template.sh`)
- Plugin-rename feature

## Tasks

### Phase 1 — BA

- [ ] **BA-W4c-B-50**: UX requirement spec — какой формат меню, summary, confirm prompt; AC для каждого UX элемента; non-interactive fallback. Артефакт: `docs/requirements/init-ux.md`

### Phase 2 — SA

- [ ] **SA-W4c-B-60**: Spec реализации — какие функции в init.sh, формат YAML парсинга manifest fields (description, audience, operations count, agent_overrides count, subagents breakdown), tests structure. Артефакт: `docs/architecture/spec-init-ux.md`

### Phase 3 — Dev

- [ ] **DEV-W4c-B-70**: Реализовать в `scripts/init.sh`:
  - Меню профилей с описаниями
  - Post-selection summary
  - Confirm prompt с `INIT_FORCE=1` bypass
- [ ] **DEV-W4c-B-71**: T-W4c-B-* assertions в `scripts/test-template.sh`:
  - Меню показывает description (grep-проверка output для каждого профиля)
  - Summary показывает operations count
  - INIT_FORCE=1 пропускает confirm
  - Non-interactive (no TTY) пропускает confirm

### Phase 4 — Docs & Smoke

- [ ] **DEV-W4c-B-72**: Update `docs/extending.md` (как добавить description в новый профиль) + `CLAUDE.md` команды раздел
- [ ] **DEV-W4c-B-73**: Final `bash scripts/check.sh --full` зелёный (213 + ~6-8 новых = ~220)
- [ ] **PM-W4c-B-74**: lessons-learned + memory update

## Dependency graph

```
BA-50 → SA-60 → DEV-70 → DEV-71 → DEV-72 → DEV-73 → PM-74
```

Sequential — небольшой scope, нет смысла параллелизировать.

## GO-критерии

- `bash scripts/init.sh` показывает меню с описанием и аудиторией для каждого профиля
- После выбора — summary block (operations, overrides, subagents)
- Confirm prompt; `INIT_FORCE=1` bypasses; non-interactive bypasses
- `scripts/check.sh --full` зелёный (~220 ассертов)
- Documentation updated

## Reference

- Эталон существующих init_prompts: scripts/init.sh:148-210 (W3-A1)
- Manifests с descriptions: docs/overlays/profiles/*/manifest.yaml (все 7 имеют description)
- W4c-A lessons: docs/lessons-learned.md row 2026-05-07 W4c-A
