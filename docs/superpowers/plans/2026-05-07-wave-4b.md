# Wave 4b Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Минимизировать baseline `content/` до одного `_index.md`; все 7 профилей через `op:add content-scaffold/` декларируют полный scaffold. Параллельно закрыть W4a Medium follow-up'ы (`ProfileError` unification, `ops_count` optimize, drop `role_status` field, verify-tasks для уже-сделанного).

**Architecture:** Удалить `content/{00-project,10-domain,30-requirements,40-architecture,60-implementation,70-operations}/` из репозитория; обогатить `docs/overlays/profiles/project/content-scaffold/` отсутствующим `10-domain/research/_index.md`; снять 5+6 `op:delete` из `kb-team`/`kb-product` манифестов; зарегенерировать 3 example каталога; в `_apply_profile.py` заменить `sys.exit(1)` на `raise ProfileError` в двух функциях; убрать лишний `python3` invocation для `ops_count` в `apply-overlay.sh`; убрать dead-field `role_status` из manifests.

**Tech Stack:** Python 3 (helpers), Bash (apply-overlay), YAML manifests, Gramax markdown.

**Spec:** `docs/superpowers/specs/2026-05-07-wave-4b-design.md`

**Reality-check корректировки spec'а:**
- T9 (validate-content baseline check) — **verify-only**: validate-content.py не зашит на baseline-имена (проверено grep'ом).
- T12 (multiline reason strip) — **verify-only**: `_apply_profile.py:207` уже делает `.replace("\n", " ").replace("\r", " ").strip()` — закрыто в W4a.
- T14 (drop `profile_dir` из `compute_verdict`) — **verify-only**: сигнатура `compute_verdict(op, init)` уже без `profile_dir`.

Verify-only задачи всё равно требуют коммита («verified, no changes») для трассировки в lessons.

---

## File Structure

| Файл | Тип изменения | Задачи |
|------|---------------|--------|
| `content/{00-project,10-domain,30-requirements,40-architecture,60-implementation,70-operations}/` | Delete (recursive) | T2 |
| `content/_index.md` | Untouched | — |
| `docs/overlays/profiles/project/content-scaffold/10-domain/research/_index.md` | Create | T1 |
| `docs/overlays/profiles/kb-team/manifest.yaml` | Modify (remove 5 op:delete) | T3 |
| `docs/overlays/profiles/kb-product/manifest.yaml` | Modify (remove 6 op:delete) | T4 |
| `examples/project-example/content/` | Regenerate | T5 |
| `examples/kb-team-example/content/` | Regenerate | T6 |
| `examples/kb-product-example/content/` | Regenerate | T7 |
| `scripts/test-template.sh` | Modify (add baseline-size assertion) | T8 |
| `scripts/validate-content.py` | Verify-only (grep audit) | T9 |
| `scripts/_apply_profile.py` | Modify (`raise ProfileError`) | T10 |
| `scripts/apply-overlay.sh` | Modify (`wc -l` вместо python3 invocation) | T11 |
| `scripts/_apply_profile.py` | Verify-only (multiline strip) | T12 |
| `docs/overlays/profiles/{kb-team,kb-product}/manifest.yaml`, test fixtures | Modify (remove `role_status: core`) | T13 |
| `scripts/_apply_profile.py` | Verify-only (compute_verdict signature) | T14 |
| `scripts/check.sh` smoke output | Verify | T15 |
| `docs/lessons-learned.md` | Modify (W4b retrospective) | T16 |

---

## Phase P1 — Baseline migration

### Task 1: Audit content-scaffold completeness

**Files:**
- Create: `docs/overlays/profiles/project/content-scaffold/10-domain/research/_index.md`

**Background:** Baseline сейчас содержит `content/10-domain/research/_index.md`, который **не** дублируется в `docs/overlays/profiles/project/content-scaffold/10-domain/`. После T2 (delete baseline) этот файл будет потерян. T1 мигрирует его в project content-scaffold.

- [ ] **Step 1: Сравнить содержимое research/_index.md из baseline и project content-scaffold**

```bash
# Источник в baseline
cat content/10-domain/research/_index.md
# Цель — этот путь должен отсутствовать
ls docs/overlays/profiles/project/content-scaffold/10-domain/research/ 2>&1
```

Expected: первая команда показывает контент; вторая — `No such file or directory`.

- [ ] **Step 2: Скопировать файл в content-scaffold**

```bash
mkdir -p docs/overlays/profiles/project/content-scaffold/10-domain/research
cp content/10-domain/research/_index.md \
   docs/overlays/profiles/project/content-scaffold/10-domain/research/_index.md
```

- [ ] **Step 3: Audit остальных baseline `_index.md` против content-scaffold**

Для каждого baseline `_index.md` проверить, есть ли копия в `docs/overlays/profiles/project/content-scaffold/`:

```bash
for f in content/00-project/_index.md \
         content/00-project/adr/_index.md \
         content/10-domain/_index.md \
         content/30-requirements/_index.md \
         content/30-requirements/functional/_index.md \
         content/30-requirements/non-functional/_index.md \
         content/40-architecture/_index.md \
         content/60-implementation/_index.md \
         content/70-operations/_index.md \
         content/_index.md; do
  rel="${f#content/}"
  scaffold="docs/overlays/profiles/project/content-scaffold/$rel"
  if [[ ! -f "$scaffold" ]]; then
    echo "MISSING in scaffold: $rel"
  elif ! diff -q "$f" "$scaffold" >/dev/null; then
    echo "DIFFERS: $rel"
  fi
done
```

Expected (после Step 2): только `content/_index.md` отсутствует в scaffold (это ОК — baseline keeps it). Остальные либо `DIFFERS` (тогда мигрировать), либо identical.

- [ ] **Step 4: Если на Step 3 нашлись `DIFFERS` — мигрировать актуальную baseline-версию в scaffold**

Для каждого `DIFFERS`:

```bash
cp content/<path>/_index.md docs/overlays/profiles/project/content-scaffold/<path>/_index.md
```

(скрипт-заглушка — реально выполнить только для тех путей, что показал Step 3).

- [ ] **Step 5: Verify**

```bash
ls docs/overlays/profiles/project/content-scaffold/10-domain/research/_index.md
diff content/10-domain/research/_index.md \
     docs/overlays/profiles/project/content-scaffold/10-domain/research/_index.md
```

Expected: file exists, diff пустой.

- [ ] **Step 6: Commit**

```bash
git add docs/overlays/profiles/project/content-scaffold/
git commit -m "$(cat <<'EOF'
feat(content-scaffold): migrate research/_index.md from baseline [W4b-T1]

Pre-W4b baseline содержит 10-domain/research/_index.md которого нет в project
content-scaffold. Перед удалением baseline (T2) копируем в scaffold чтобы
project-профиль продолжал давать research/ через op:add.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: Delete baseline content folders

**Files:**
- Delete: `content/00-project/`, `content/10-domain/`, `content/30-requirements/`, `content/40-architecture/`, `content/60-implementation/`, `content/70-operations/`

**Depends on:** T1.

- [ ] **Step 1: Verify baseline state**

```bash
git ls-files content/ | wc -l
```

Expected: 11 (все `_index.md`, см. spec §2.1).

- [ ] **Step 2: Delete folders**

```bash
git rm -r content/00-project/ \
            content/10-domain/ \
            content/30-requirements/ \
            content/40-architecture/ \
            content/60-implementation/ \
            content/70-operations/
```

- [ ] **Step 3: Verify final state**

```bash
git ls-files content/
```

Expected: ровно одна строка — `content/_index.md`.

- [ ] **Step 4: Manual smoke — apply-overlay project на временный target**

```bash
TMP=$(mktemp -d)
mkdir -p "$TMP/content"
cp content/_index.md "$TMP/content/"
cd "$TMP"
ln -s "$OLDPWD/docs" docs
ln -s "$OLDPWD/scripts" scripts
ln -s "$OLDPWD/.claude" .claude
bash scripts/apply-overlay.sh --profile project --init 2>&1 | tail -20
ls -la content/
cd "$OLDPWD"
rm -rf "$TMP"
```

Expected: project profile применяется без ошибок; `$TMP/content/` содержит полный project scaffold (00-project/, 10-domain/research/_index.md, и т.д.).

- [ ] **Step 5: Commit**

```bash
git commit -m "$(cat <<'EOF'
refactor(baseline): drop delivery-структуру из content/ baseline [W4b-T2]

Удалены 6 папок (00-project/, 10-domain/, 30-requirements/, 40-architecture/,
60-implementation/, 70-operations/) и их 9 _index.md файлов. Baseline теперь
ровно один файл — content/_index.md.

Все 7 профилей получают полный scaffold через op:add content-scaffold/.
project-профиль уже декларирует op:add — без изменений в манифесте. kb-team
и kb-product теряют необходимость в op:delete (закрывается в T3, T4).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: Drop op:delete from kb-team manifest

**Files:**
- Modify: `docs/overlays/profiles/kb-team/manifest.yaml`

**Depends on:** T2.

- [ ] **Step 1: Read current manifest**

Прочитать `docs/overlays/profiles/kb-team/manifest.yaml`. Текущая `operations:` секция:

```yaml
operations:
  - op: delete
    target: content/00-project/
    reason: "kb-team не использует delivery-структуру (нет ADR, plans, critical-path, security)"
  - op: delete
    target: content/30-requirements/
    reason: "kb-team не использует функциональные требования"
  - op: delete
    target: content/40-architecture/
    reason: "kb-team не имеет компонентов архитектуры"
  - op: delete
    target: content/60-implementation/
    reason: "нет реализации"
  - op: delete
    target: content/70-operations/
    reason: "переносится в 30-runbooks/"
  - op: add
    source: content-scaffold/
    target: content/
    reason: "kb-team scaffold (10-domain, 20-onboarding, 30-runbooks, 40-roles, 50-incidents)"
  - op: replace
    source: doc-root.yaml
    target: content/.doc-root.yaml
    reason: "kb-team properties (Owner, Эскалация)"
```

- [ ] **Step 2: Modify — удалить 5 op:delete записей, сохранить op:add и op:replace**

Целевая `operations:` секция:

```yaml
operations:
  - op: add
    source: content-scaffold/
    target: content/
    reason: "kb-team scaffold (10-domain, 20-onboarding, 30-runbooks, 40-roles, 50-incidents)"
  - op: replace
    source: doc-root.yaml
    target: content/.doc-root.yaml
    reason: "kb-team properties (Owner, Эскалация)"
```

- [ ] **Step 3: Verify manifest корректен**

```bash
python3 scripts/validate-profile.py docs/overlays/profiles/kb-team/
```

Expected: `Profiles: OK (1 проверено)`.

- [ ] **Step 4: Smoke — apply на TMP**

```bash
TMP=$(mktemp -d)
mkdir -p "$TMP/content"
cp content/_index.md "$TMP/content/"
cd "$TMP"
ln -s "$OLDPWD/docs" docs
ln -s "$OLDPWD/scripts" scripts
ln -s "$OLDPWD/.claude" .claude
bash scripts/apply-overlay.sh --profile kb-team --init 2>&1 | tail -20
ls content/
cd "$OLDPWD"
rm -rf "$TMP"
```

Expected: kb-team scaffold без warnings про "REFUSED delete" (которые сейчас могут возникать если baseline пустой).

- [ ] **Step 5: Commit**

```bash
git add docs/overlays/profiles/kb-team/manifest.yaml
git commit -m "$(cat <<'EOF'
refactor(kb-team): drop 5 op:delete после baseline minimization [W4b-T3]

После W4b-T2 baseline content/ пуст (только _index.md), 5 op:delete (00-project,
30-requirements, 40-architecture, 60-implementation, 70-operations) стали no-op.
Удалены, чтобы манифест отражал реальные операции профиля.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: Drop op:delete from kb-product manifest

**Files:**
- Modify: `docs/overlays/profiles/kb-product/manifest.yaml`

**Depends on:** T2. Может выполняться параллельно с T3 — разные файлы.

- [ ] **Step 1: Read current manifest**

Текущая `operations:` секция содержит 6 op:delete (00-project, 10-domain, 30-requirements, 40-architecture, 60-implementation, 70-operations) + op:add + op:replace.

- [ ] **Step 2: Modify — удалить 6 op:delete, сохранить op:add и op:replace**

Целевая `operations:`:

```yaml
operations:
  - op: add
    source: content-scaffold/
    target: content/
    reason: "kb-product scaffold (getting-started, guides, reference, troubleshooting)"
  - op: replace
    source: doc-root.yaml
    target: content/.doc-root.yaml
    reason: "kb-product properties (Тип контента, Версия продукта, Аудитория)"
```

- [ ] **Step 3: Verify manifest**

```bash
python3 scripts/validate-profile.py docs/overlays/profiles/kb-product/
```

Expected: `Profiles: OK`.

- [ ] **Step 4: Smoke — apply на TMP**

```bash
TMP=$(mktemp -d)
mkdir -p "$TMP/content"
cp content/_index.md "$TMP/content/"
cd "$TMP"
ln -s "$OLDPWD/docs" docs
ln -s "$OLDPWD/scripts" scripts
ln -s "$OLDPWD/.claude" .claude
bash scripts/apply-overlay.sh --profile kb-product --init 2>&1 | tail -20
ls content/
cd "$OLDPWD"
rm -rf "$TMP"
```

Expected: kb-product scaffold (getting-started/, guides/, reference/, troubleshooting/) без warnings про "REFUSED delete".

- [ ] **Step 5: Commit**

```bash
git add docs/overlays/profiles/kb-product/manifest.yaml
git commit -m "$(cat <<'EOF'
refactor(kb-product): drop 6 op:delete после baseline minimization [W4b-T4]

Аналогично W4b-T3: после T2 baseline пуст, 6 op:delete стали no-op. Удалены.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Phase P2 — Examples regeneration + tests update

### Task 5: Regenerate examples/project-example/

**Files:**
- Regenerate: `examples/project-example/content/`

**Depends on:** T2.

- [ ] **Step 1: Запомнить старое состояние для diff**

```bash
git ls-files examples/project-example/ > /tmp/w4b-project-example-before.txt
wc -l /tmp/w4b-project-example-before.txt
```

- [ ] **Step 2: Удалить старый каталог**

```bash
rm -rf examples/project-example
```

- [ ] **Step 3: Зарегенерировать через apply-overlay --init**

```bash
mkdir -p examples/project-example/content
cp content/_index.md examples/project-example/content/
cd examples/project-example
ln -s ../../docs docs
ln -s ../../scripts scripts
ln -s ../../.claude .claude
bash scripts/apply-overlay.sh --profile project --init 2>&1 | tail -10
rm docs scripts .claude
cd ../..
```

- [ ] **Step 4: Verify структуру**

```bash
find examples/project-example/content -name "_index.md" | sort
ls examples/project-example/content/.doc-root.yaml
```

Expected: scaffold project-профиля скопирован (`00-project/{_index,adr,plans,critical-path,security,compliance}/`, `10-domain/{_index,research,glossary.md}`, `30-requirements/{_index,functional,non-functional}`, `40-architecture/`, `60-implementation/{_index,test-reports}`, `70-operations/`); `.doc-root.yaml` присутствует.

- [ ] **Step 5: Diff vs прошлое состояние**

```bash
git ls-files examples/project-example/ > /tmp/w4b-project-example-after.txt
diff /tmp/w4b-project-example-before.txt /tmp/w4b-project-example-after.txt | head -30
```

Expected: diff показывает либо identical (если scaffold идентичен прошлой регенерации), либо только добавление `10-domain/research/_index.md` (от T1) — никаких unrelated изменений.

- [ ] **Step 6: Commit**

```bash
git add examples/project-example/
git commit -m "$(cat <<'EOF'
chore(examples): regenerate project-example post-W4b-T2 [W4b-T5]

После baseline minimization регенерация проверяет что project профиль
выдаёт идентичный scaffold через op:add content-scaffold/. Diff показывает
только эффект T1 (research/_index.md мигрировал из baseline в scaffold).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: Regenerate examples/kb-team-example/

**Files:**
- Regenerate: `examples/kb-team-example/content/`

**Depends on:** T3. Может параллельно с T5 и T7.

- [ ] **Step 1: Запомнить состояние**

```bash
git ls-files examples/kb-team-example/ > /tmp/w4b-kb-team-example-before.txt
```

- [ ] **Step 2: Удалить и регенерировать**

```bash
rm -rf examples/kb-team-example
mkdir -p examples/kb-team-example/content
cp content/_index.md examples/kb-team-example/content/
cd examples/kb-team-example
ln -s ../../docs docs
ln -s ../../scripts scripts
ln -s ../../.claude .claude
bash scripts/apply-overlay.sh --profile kb-team --init 2>&1 | tail -10
rm docs scripts .claude
cd ../..
```

- [ ] **Step 3: Verify структуру**

```bash
find examples/kb-team-example/content -name "_index.md" | sort
```

Expected: kb-team scaffold (`10-domain/`, `20-onboarding/`, `30-runbooks/`, `40-roles/`, `50-incidents/`). НЕТ delivery-папок (`00-project/`, `30-requirements/`, `40-architecture/`, `60-implementation/`, `70-operations/`).

- [ ] **Step 4: Diff vs before**

```bash
git ls-files examples/kb-team-example/ > /tmp/w4b-kb-team-example-after.txt
diff /tmp/w4b-kb-team-example-before.txt /tmp/w4b-kb-team-example-after.txt
```

Expected: identical (kb-team даёт тот же scaffold что и раньше; разница только в том, что он строится через op:add без преходящих op:delete).

- [ ] **Step 5: Commit**

```bash
git add examples/kb-team-example/
git commit -m "$(cat <<'EOF'
chore(examples): regenerate kb-team-example post-W4b-T3 [W4b-T6]

После W4b-T3 (drop 5 op:delete) регенерация проверяет что kb-team scaffold
без op:delete даёт идентичную итоговую структуру.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: Regenerate examples/kb-product-example/

**Files:**
- Regenerate: `examples/kb-product-example/content/`, `examples/kb-product-example/.claude/plugins/`

**Depends on:** T4. Может параллельно с T5, T6.

- [ ] **Step 1: Запомнить состояние**

```bash
git ls-files examples/kb-product-example/ > /tmp/w4b-kb-product-example-before.txt
```

- [ ] **Step 2: Удалить и регенерировать**

```bash
rm -rf examples/kb-product-example
mkdir -p examples/kb-product-example/content
cp content/_index.md examples/kb-product-example/content/
cd examples/kb-product-example
ln -s ../../docs docs
ln -s ../../scripts scripts
ln -s ../../.claude .claude
bash scripts/apply-overlay.sh --profile kb-product --init 2>&1 | tail -10
rm docs scripts .claude
cd ../..
```

Note: kb-product manifest содержит `agent_overrides:` → apply-overlay вызовет `_resolve_agents.py`. Нужно убедиться, что resolved agent prompts попадают в `.claude/plugins/project/agents/` относительно target dir (а не от repo root). Если resolved prompts затронут `examples/kb-product-example/.claude/...` — это OK для example.

- [ ] **Step 3: Verify**

```bash
find examples/kb-product-example/content -name "_index.md" | sort
ls examples/kb-product-example/.claude/plugins/ 2>/dev/null
```

Expected: kb-product scaffold (`getting-started/_index.md`, `guides/_index.md`, `reference/_index.md`, `troubleshooting/_index.md`); resolved tech-writer prompt в `.claude/plugins/.../tech-writer-agent.md`.

- [ ] **Step 4: Diff vs before**

```bash
git ls-files examples/kb-product-example/ > /tmp/w4b-kb-product-example-after.txt
diff /tmp/w4b-kb-product-example-before.txt /tmp/w4b-kb-product-example-after.txt
```

Expected: identical или только timestamp-related поля в resolved agent prompt (если resolver их добавляет).

- [ ] **Step 5: Commit**

```bash
git add examples/kb-product-example/
git commit -m "$(cat <<'EOF'
chore(examples): regenerate kb-product-example post-W4b-T4 [W4b-T7]

После W4b-T4 (drop 6 op:delete) регенерация даёт идентичную итоговую
структуру + resolved agent prompts (W4a-T16 customer-facing tech-writer).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 8: Update test-template.sh assertions

**Files:**
- Modify: `scripts/test-template.sh`

**Depends on:** T2-T4 (для baseline-size assertion); T5-T7 (для example assertions если они там есть).

**Background:** Текущие assertions на lines 66-78 проверяют наличие `content/00-project/_index.md` и т.д. **post-init**. После W4b они **по-прежнему** должны проходить — `init.sh` запускает `apply-overlay --profile project --init`, который через op:add создаёт scaffold. Реальное изменение: добавить новый pre-init assertion «git ls-files content/ == 1».

- [ ] **Step 1: Read контекст test-template.sh**

```bash
sed -n '50,90p' scripts/test-template.sh
```

Найти assertion-блок где проверяются post-init `content/...` пути (lines ~66-78).

- [ ] **Step 2: Найти место для baseline-size pre-init assertion**

Нужен раздел теста, который проверяет состояние **до** init.sh. Если его нет — добавить новый раздел в начале файла после `assert` функции.

- [ ] **Step 3: Add assertion**

Добавить (например, после блока с `assert "PROJECT_NAME replaced"` или в отдельный pre-init test):

```bash
# T-W4b-BASELINE: baseline content/ — ровно один файл (_index.md)
echo "[T-W4b] baseline content/ minimization check"
assert "T-W4b-BASELINE: content/ baseline = 1 файл (_index.md)" \
       "[ \"\$(cd \"\$REPO_ROOT\" && git ls-files content/ | wc -l | tr -d ' ')\" = '1' ]"
assert "T-W4b-BASELINE: единственный файл = content/_index.md" \
       "[ \"\$(cd \"\$REPO_ROOT\" && git ls-files content/)\" = 'content/_index.md' ]"
```

(`$REPO_ROOT` — переменная, существующая в test-template.sh; если нет, использовать корневой path напрямую.)

- [ ] **Step 4: Запустить test-template**

```bash
bash scripts/test-template.sh 2>&1 | tail -30
```

Expected: все assertions зелёные, включая 2 новых T-W4b-BASELINE.

- [ ] **Step 5: Запустить full smoke**

```bash
bash scripts/check.sh --full 2>&1 | tail -20
```

Expected: `OK` без `FAIL`. Число ассертов увеличилось на 2.

- [ ] **Step 6: Commit**

```bash
git add scripts/test-template.sh
git commit -m "$(cat <<'EOF'
test(test-template): T-W4b-BASELINE assertions для baseline minimization [W4b-T8]

Проверяет что content/ baseline в репозитории — ровно один файл
(content/_index.md). Защищает от регрессии: если кто-то закоммитит
content/<folder>/_index.md, тест упадёт.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 9: Verify validate-content.py independence от baseline

**Files:**
- Verify-only: `scripts/validate-content.py`

**Depends on:** T2.

- [ ] **Step 1: Grep на baseline-имена в validate-content.py**

```bash
grep -nE "00-project|10-domain|30-requirements|40-architecture|60-implementation|70-operations" \
     scripts/validate-content.py
```

Expected: пустой output (validator не зашит на baseline-имена).

- [ ] **Step 2: Run validate-content на пустом baseline**

```bash
python3 scripts/validate-content.py content/
```

Expected: `OK (1 файлов проверены)` — validator принимает baseline из 1 файла.

- [ ] **Step 3: Run validate-content на каждом example**

```bash
for ex in project-example kb-team-example kb-product-example; do
  echo "=== $ex ==="
  python3 scripts/validate-content.py "examples/$ex/content/"
done
```

Expected: все три зелёные.

- [ ] **Step 4: Commit (verify-only marker)**

```bash
git commit --allow-empty -m "$(cat <<'EOF'
chore(verify): validate-content.py не зависит от baseline-имён [W4b-T9]

Reality-check spec'а: validate-content.py валидирует только Gramax convention
(_index.md в каждой подпапке с .md, frontmatter object-нотация, property
names declared в .doc-root.yaml). Никаких захардкоженных проверок baseline-
папок (00-project/, 10-domain/, ...). После W4b-T2 (baseline minimization)
валидатор продолжает работать без изменений.

Verified:
- grep baseline-имён в validate-content.py → empty
- validate-content.py content/ → OK (1 файл)
- validate-content.py examples/{project,kb-team,kb-product}-example/content/ → OK

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Phase P3 — W4a Medium cleanup

P3 не зависит от P1/P2. Запускается параллельно с P1 на старте W4b.

**Внутри P3 sequencing:** T10 → T12 → T14 трогают `_apply_profile.py` — последовательно. T11 и T13 — параллельные потоки.

### Task 10: Unify ProfileError in _apply_profile.py

**Files:**
- Modify: `scripts/_apply_profile.py`
- Test: добавить негативный smoke в `scripts/test-apply-overlay.sh` (или создать новый `scripts/test-apply-profile.sh` если отсутствует)

**Background:** Текущий `_apply_profile.py` использует `raise ManifestError` для load_manifest, но `sys.exit(1)` для двух других функций (`apply_on_value_mutations:152`, `apply_dotted_mutation:176,184,190`). Цель — единый style: всё через `raise ProfileError` (расширение существующего `ManifestError` или новый базовый класс).

- [ ] **Step 1: Найти текущие тесты apply-overlay**

```bash
grep -n "apply_on_value\|apply_dotted\|broken manifest" scripts/test-apply-overlay.sh 2>/dev/null | head
```

Если нет специфичного теста — добавить ниже.

- [ ] **Step 2: Написать failing negative test**

Добавить в `scripts/test-apply-overlay.sh` (в конец файла перед summary):

```bash
echo "[T-W4b-T10] negative: apply_dotted_mutation на несуществующий path"
TMP_T10=$(mktemp -d)
mkdir -p "$TMP_T10/docs/overlays/profiles/test-bad-mutation"
cat > "$TMP_T10/docs/overlays/profiles/test-bad-mutation/manifest.yaml" <<EOF
schema_version: 1
name: test-bad-mutation
description: typo'ed mutation path для negative test
audience: tests
status: stub
subagents: {}
pipelines: {}
init_prompts:
  - id: bad
    type: enum
    choices: [yes]
    default: yes
    on_value:
      yes:
        nonexistent.section: core
operations: []
compatible_stacks: []
maintainer: tests
EOF
ln -s "$OLDPWD/scripts" "$TMP_T10/scripts"
cd "$TMP_T10"
INIT_PROMPT_bad=yes python3 scripts/_apply_profile.py docs/overlays/profiles/test-bad-mutation 2>&1 >/dev/null
rc=$?
cd "$OLDPWD"
rm -rf "$TMP_T10"
assert "T-W4b-T10: bad mutation → exit code 1" "[ \"$rc\" = '1' ]"
```

Run: `bash scripts/test-apply-overlay.sh 2>&1 | tail -10`
Expected: assertion проходит на текущем коде (sys.exit(1) даёт exit 1) — это base level.

- [ ] **Step 3: Написать second failing test — на тип exception (TDD red phase для refactor'а)**

Добавить отдельный pytest-style тест или inline Python check:

```bash
echo "[T-W4b-T10] negative: ProfileError raised, не SystemExit"
TMP_T10B=$(mktemp -d)
mkdir -p "$TMP_T10B/docs/overlays/profiles/test-bad-mutation-2"
cat > "$TMP_T10B/docs/overlays/profiles/test-bad-mutation-2/manifest.yaml" <<EOF
schema_version: 1
name: test-bad-mutation-2
description: typo'ed mutation path
audience: tests
status: stub
subagents: {}
pipelines: {}
init_prompts:
  - id: bad
    type: enum
    choices: [yes]
    default: yes
    on_value:
      yes:
        nonexistent.section: core
operations: []
compatible_stacks: []
maintainer: tests
EOF
cd "$TMP_T10B"
ln -s "$OLDPWD/scripts" scripts

INIT_PROMPT_bad=yes python3 -c "
import sys
sys.path.insert(0, 'scripts')
import importlib.util
spec = importlib.util.spec_from_file_location('apply_profile', 'scripts/_apply_profile.py')
mod = importlib.util.module_from_spec(spec)
spec.loader.exec_module(mod)
try:
    manifest = mod.load_manifest(__import__('pathlib').Path('docs/overlays/profiles/test-bad-mutation-2'))
    mod.apply_on_value_mutations(manifest)
    print('NO_EXCEPTION')
except mod.ProfileError as e:
    print('OK_ProfileError')
except SystemExit:
    print('FAIL_SystemExit')
" 2>&1 | tail -1
result=$?
cd "$OLDPWD"
rm -rf "$TMP_T10B"
```

Run: первый запуск выдаст `FAIL_SystemExit` (потому что код использует sys.exit). Это **red** для TDD.

Expected (до Step 4): `FAIL_SystemExit`.

- [ ] **Step 4: Refactor `_apply_profile.py` — заменить sys.exit на raise ProfileError**

Прочитать `scripts/_apply_profile.py` lines 30-32 (текущий ManifestError):

```python
class ManifestError(Exception):
    """Manifest load/parse error — caller should catch and exit cleanly."""
```

**Change 1:** Переименовать `ManifestError` → `ProfileError`, либо добавить alias. Рекомендация: переименовать + сохранить alias для обратной совместимости.

```python
class ProfileError(Exception):
    """Profile manifest load/parse/mutation error — caller should catch and exit cleanly."""


# Backwards-compat alias (W4a callers использовали ManifestError)
ManifestError = ProfileError
```

**Change 2:** В `apply_on_value_mutations` (line ~145-152) заменить:

```python
        if prompt_type == "enum":
            choices = prompt.get("choices") or []
            if value not in choices:
                print(
                    f"ERROR: {env_key}={value!r} не в choices {choices}",
                    file=sys.stderr,
                )
                sys.exit(1)
```

на:

```python
        if prompt_type == "enum":
            choices = prompt.get("choices") or []
            if value not in choices:
                raise ProfileError(
                    f"{env_key}={value!r} не в choices {choices}"
                )
```

**Change 3:** В `apply_dotted_mutation` (lines ~170-191) заменить три `sys.exit(1)` на `raise ProfileError(...)`:

```python
def apply_dotted_mutation(manifest: dict, dotted_path: str, new_value) -> None:
    """Применяет mutation по dotted-path (например 'subagents.compliance' → 'core').

    Raises ProfileError если path не существует (защита от typo в manifest).
    """
    parts = dotted_path.split(".")
    if len(parts) < 2:
        raise ProfileError(
            f"invalid dotted path '{dotted_path}' (нужно минимум section.key)"
        )
    section_name, key = parts[0], ".".join(parts[1:])
    section = manifest.get(section_name)
    if not isinstance(section, dict):
        raise ProfileError(
            f"section '{section_name}' не существует в manifest или не dict"
        )
    if key not in section:
        raise ProfileError(
            f"'{dotted_path}' не существует в manifest (нет ключа '{key}' в '{section_name}')"
        )
    section[key] = new_value
```

**Change 4:** В `main` (line ~248-253) расширить except для apply_on_value_mutations:

```python
    try:
        manifest = load_manifest(profile_dir)
        apply_on_value_mutations(manifest)  # T6: env-driven mutations
    except ProfileError as e:
        print(f"ERROR: {e}", file=sys.stderr)
        return 1
    plan = emit_plan(manifest, args.init)
```

(Перенести `apply_on_value_mutations` внутрь try-блока вместе с `load_manifest`.)

- [ ] **Step 5: Re-run negative test (Step 3)**

Expected: вывод `OK_ProfileError`.

- [ ] **Step 6: Re-run базовый negative test (Step 2)**

Run: `bash scripts/test-apply-overlay.sh 2>&1 | tail -5`
Expected: assertion `T-W4b-T10: bad mutation → exit code 1` всё ещё проходит (raise → main ловит → return 1 → exit 1).

- [ ] **Step 7: Smoke — apply правильного профиля не сломан**

```bash
bash scripts/apply-overlay.sh --profile project --dry-run 2>&1 | tail -5
```

Expected: dry-run проходит, JSON plan печатается.

- [ ] **Step 8: Full validate**

```bash
python3 scripts/validate-profile.py
```

Expected: все профили `OK`.

- [ ] **Step 9: Commit**

```bash
git add scripts/_apply_profile.py scripts/test-apply-overlay.sh
git commit -m "$(cat <<'EOF'
refactor(_apply_profile): unified ProfileError вместо sys.exit [W4b-T10]

apply_on_value_mutations и apply_dotted_mutation теперь raise ProfileError
вместо прямого sys.exit(1). main() единый except-блок ловит и emit-ит exit
code 1.

ManifestError остаётся как alias для backwards-compat (W4a callers).

Tests:
- T-W4b-T10: bad dotted path → exit code 1 (interface-level)
- T-W4b-T10b: ProfileError exception type (не SystemExit)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 11: Optimize ops_count в apply-overlay.sh

**Files:**
- Modify: `scripts/apply-overlay.sh`

**Depends on:** none (independent от P1, P2, T10).

**Background:** В `apply-overlay.sh:371` отдельный `python3 -c "import json..."` парсит уже-сгенерированный JSON только чтобы достать `ops_count` поле. Helper `_apply_profile.py` уже эмиттит `ops_count` в JSON top-level (line 230). Можно убрать invocation, посчитав строки в `plan_tsv` (которое и так генерируется ниже).

- [ ] **Step 1: Read контекст**

```bash
sed -n '365,400p' scripts/apply-overlay.sh
```

Текущий код:

```bash
  # W4a-T2: ops_count emit'ится helper'ом в JSON top-level — без дополнительного python invocation
  local ops_count
  ops_count=$(echo "$plan_json" | python3 -c "import json, sys; print(json.load(sys.stdin)['ops_count'])")

  if [[ "$ops_count" -eq 0 ]]; then
    echo "No operations defined — done."
    return 0
  fi

  echo "Operations to execute: $ops_count"

  # Convert JSON plan to delimited rows для bash-friendly iteration.
  ...
  plan_tsv=$(echo "$plan_json" | python3 -c '
import json, sys
plan = json.load(sys.stdin)
US = "\x1f"
for op in plan["ops"]:
    print(US.join([
        op.get("op", ""),
        ...
    ]))
')
```

Заметка про W4a-T2 в комментарии **не соответствует** коду — там реально ещё python invocation. Камент написан как «ops_count emit'ится helper'ом без дополнительного python invocation», но в строке ниже именно дополнительный python invocation. Это W4b-T11 fix.

- [ ] **Step 2: Перенести ops_count после plan_tsv генерации, вычислять через wc -l**

Заменить блок lines 369-378:

```bash
  # W4a-T2: ops_count emit'ится helper'ом в JSON top-level — без дополнительного python invocation
  local ops_count
  ops_count=$(echo "$plan_json" | python3 -c "import json, sys; print(json.load(sys.stdin)['ops_count'])")

  if [[ "$ops_count" -eq 0 ]]; then
    echo "No operations defined — done."
    return 0
  fi

  echo "Operations to execute: $ops_count"

  # Convert JSON plan to delimited rows для bash-friendly iteration.
```

на:

```bash
  # Convert JSON plan to delimited rows для bash-friendly iteration.
```

(удалить блок ops_count — он будет рассчитан позже).

И заменить блок plan_tsv с фолоwup:

```bash
  plan_tsv=$(echo "$plan_json" | python3 -c '
import json, sys
plan = json.load(sys.stdin)
US = "\x1f"
for op in plan["ops"]:
    print(US.join([
        op.get("op", ""),
        op.get("source", ""),
        op.get("target", ""),
        op.get("reason", ""),
        op.get("verdict", ""),
    ]))
')

  # W4b-T11: ops_count из TSV вместо отдельного python3 invocation
  local ops_count
  if [[ -z "$plan_tsv" ]]; then
    ops_count=0
  else
    ops_count=$(printf '%s\n' "$plan_tsv" | grep -c -v '^$' || true)
  fi

  if [[ "$ops_count" -eq 0 ]]; then
    echo "No operations defined — done."
    return 0
  fi

  echo "Operations to execute: $ops_count"

  while IFS=$'\x1f' read -r op source target reason verdict; do
```

- [ ] **Step 3: Smoke — apply project --dry-run**

```bash
bash scripts/apply-overlay.sh --profile project --dry-run 2>&1 | grep -E "Operations to execute|^\[ADD|^\[REPLACE"
```

Expected: `Operations to execute: 2`, и [ADD]/[REPLACE] логи.

- [ ] **Step 4: Smoke — empty operations (stub-профиль)**

```bash
bash scripts/apply-overlay.sh --profile course --dry-run 2>&1 | tail -5
```

Expected: `No operations defined — done.` (stub-профили имеют пустой operations).

- [ ] **Step 5: Smoke — kb-team --dry-run**

```bash
bash scripts/apply-overlay.sh --profile kb-team --dry-run 2>&1 | grep "Operations to execute"
```

Expected (после T3): `Operations to execute: 2` (op:add + op:replace).

- [ ] **Step 6: Verify — нет лишнего python invocation**

```bash
grep -n "python3 -c" scripts/apply-overlay.sh
```

Expected: 2 invocation'а (один — `import yaml; m=...status` на line ~362; второй — TSV generation block). Третий (ops_count) удалён.

- [ ] **Step 7: Run full test-apply-overlay**

```bash
bash scripts/test-apply-overlay.sh 2>&1 | tail -10
```

Expected: all green.

- [ ] **Step 8: Commit**

```bash
git add scripts/apply-overlay.sh
git commit -m "$(cat <<'EOF'
refactor(apply-overlay): drop python3 invocation для ops_count [W4b-T11]

ops_count теперь считается через wc -l после plan_tsv генерации (TSV строки
уже в bash-памяти, лишний python3 -c только для чтения JSON top-level поля).

Один python3 invocation вместо двух в счастливом пути profile apply.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 12: Verify multiline reason strip guard

**Files:**
- Verify-only: `scripts/_apply_profile.py`
- Test (опционально): добавить unit-test с multiline reason

**Depends on:** T10 (если переименование ManifestError → ProfileError повлияет на test fixtures).

**Background:** Spec упоминает «strip guard для multiline reason». Reality-check показал — `_apply_profile.py:207` уже выполняет `.replace("\n", " ").replace("\r", " ").strip()`. T12 = verify + добавить explicit unit-test.

- [ ] **Step 1: Verify код**

```bash
grep -n "replace.*\\\\n\|reason" scripts/_apply_profile.py | head
```

Expected: line 207 содержит `.replace("\n", " ").replace("\r", " ").strip()`.

- [ ] **Step 2: Написать unit-test**

Добавить в `scripts/test-apply-overlay.sh` (или отдельный test-apply-profile.sh если есть):

```bash
echo "[T-W4b-T12] multiline reason → стриппится в TSV"
TMP_T12=$(mktemp -d)
mkdir -p "$TMP_T12/docs/overlays/profiles/test-multiline-reason"
cat > "$TMP_T12/docs/overlays/profiles/test-multiline-reason/manifest.yaml" <<'EOF'
schema_version: 1
name: test-multiline-reason
description: multiline reason tests TSV emission
audience: tests
status: stub
subagents: {}
pipelines: {}
content_scaffold: scaffold/
operations:
  - op: add
    source: scaffold/
    target: content/
    reason: |
      first line
      second line
      third
init_prompts: []
compatible_stacks: []
maintainer: tests
EOF
mkdir -p "$TMP_T12/docs/overlays/profiles/test-multiline-reason/scaffold"
echo "# scaffold" > "$TMP_T12/docs/overlays/profiles/test-multiline-reason/scaffold/_index.md"
cd "$TMP_T12"
ln -s "$OLDPWD/scripts" scripts
plan=$(python3 scripts/_apply_profile.py docs/overlays/profiles/test-multiline-reason)
cd "$OLDPWD"
rm -rf "$TMP_T12"

# Reason в JSON не должен содержать \n
reason_field=$(echo "$plan" | python3 -c "import json, sys; p=json.load(sys.stdin); print(p['ops'][0]['reason'])")
assert "T-W4b-T12: multiline reason стриплен (нет \\n)" "[ \"\$(echo \"$reason_field\" | wc -l | tr -d ' ')\" = '1' ]"
assert "T-W4b-T12: multiline reason содержит части" "echo \"$reason_field\" | grep -q 'first line second line third'"
```

- [ ] **Step 3: Run test**

```bash
bash scripts/test-apply-overlay.sh 2>&1 | grep -E "T-W4b-T12"
```

Expected: 2 assertions pass.

- [ ] **Step 4: Commit**

```bash
git add scripts/test-apply-overlay.sh
git commit -m "$(cat <<'EOF'
test(apply-overlay): explicit multiline reason TSV-safety [W4b-T12]

Reality-check: _apply_profile.py:207 уже делает .replace для \\n и \\r
(заложено в W4a). T12 добавляет explicit assertion чтобы регрессия (если
кто-то уберёт strip) была поймана сразу.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 13: Drop role_status field

**Files:**
- Modify: `docs/overlays/profiles/kb-team/manifest.yaml`
- Modify: `docs/overlays/profiles/kb-product/manifest.yaml`
- Modify: `scripts/test-validate-profile.sh` (5 fixture occurrences)
- Modify: `scripts/test-resolve-agents.sh` (1 fixture occurrence)
- Verify: `scripts/validate-profile.py` (no references — confirmed by grep)

**Depends on:** none (independent от P1, P2, T10, T11).

**Background:** `role_status: core` field присутствует в `agent_overrides.<role>.role_status` записях, но `validate-profile.py` его не читает (M11.4 валидирует только `subagents.role`). Field — dead. Drop.

- [ ] **Step 1: Verify validate-profile.py не использует role_status**

```bash
grep -n "role_status" scripts/validate-profile.py
```

Expected: empty output.

- [ ] **Step 2: Modify kb-team manifest**

`docs/overlays/profiles/kb-team/manifest.yaml` — найти секцию:

```yaml
agent_overrides:
  tech-writer:
    source: agent-overrides/tech-writer.md
    role_status: core
```

Изменить на:

```yaml
agent_overrides:
  tech-writer:
    source: agent-overrides/tech-writer.md
```

- [ ] **Step 3: Modify kb-product manifest**

То же самое для `docs/overlays/profiles/kb-product/manifest.yaml`:

```yaml
agent_overrides:
  tech-writer:
    source: agent-overrides/tech-writer.md
    role_status: core
```

→

```yaml
agent_overrides:
  tech-writer:
    source: agent-overrides/tech-writer.md
```

- [ ] **Step 4: Modify test-validate-profile.sh fixtures (5 occurrences)**

```bash
grep -n "role_status: core" scripts/test-validate-profile.sh
```

5 fixture-блоков (примерно lines 504, 561, 609, 665, 721). Удалить строку `    role_status: core` из каждой fixture (строка с 4 пробелами indent — sub-key `agent_overrides.<role>`).

Используя sed (осторожно, чтобы не задеть docstring'и):

```bash
# Manual review с grep — определить точные строки, потом edit each
sed -i '' '/^    role_status: core$/d' scripts/test-validate-profile.sh
# (BSD/macOS sed; для GNU использовать sed -i без '' — но в проекте Darwin → BSD)
grep -c "role_status" scripts/test-validate-profile.sh  # должно быть 0
```

- [ ] **Step 5: Modify test-resolve-agents.sh fixture (1 occurrence)**

```bash
grep -n "role_status: core" scripts/test-resolve-agents.sh
```

Удалить эту строку аналогично:

```bash
sed -i '' '/^    role_status: core$/d' scripts/test-resolve-agents.sh
grep -c "role_status" scripts/test-resolve-agents.sh  # 0
```

- [ ] **Step 6: Verify validate-profile зелёный для всех профилей**

```bash
python3 scripts/validate-profile.py
```

Expected: `Profiles: OK (7 проверено)` — kb-team и kb-product без role_status валидны.

- [ ] **Step 7: Run all related tests**

```bash
bash scripts/test-validate-profile.sh 2>&1 | tail -5
bash scripts/test-resolve-agents.sh 2>&1 | tail -5
```

Expected: оба зелёные.

- [ ] **Step 8: Verify no remaining role_status references**

```bash
grep -rn "role_status" docs/overlays/profiles/ scripts/ docs/architecture-overview.md docs/troubleshooting.md 2>/dev/null
```

Expected: пусто (упоминания только в spec'ах W4a/W4b — это ОК, исторические документы).

- [ ] **Step 9: Commit**

```bash
git add docs/overlays/profiles/kb-team/manifest.yaml \
        docs/overlays/profiles/kb-product/manifest.yaml \
        scripts/test-validate-profile.sh \
        scripts/test-resolve-agents.sh
git commit -m "$(cat <<'EOF'
refactor(manifests): drop dead role_status field [W4b-T13]

validate-profile.py M11 валидирует только subagents.<role> — поле
agent_overrides.<role>.role_status никогда не читалось (dead field, добавлен
в W4a по умолчанию).

Удалено из:
- 2 manifests (kb-team, kb-product)
- 5 test fixtures в test-validate-profile.sh
- 1 test fixture в test-resolve-agents.sh

Manifest schema стала чище; ничто не теряется (effective role status уже
определяется через subagents.<role>).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 14: Verify compute_verdict signature не имеет profile_dir

**Files:**
- Verify-only: `scripts/_apply_profile.py`

**Depends on:** T10 (если refactor затронул сигнатуры).

**Background:** Spec упоминает «`profile_dir` reserved param в `compute_verdict`». Reality-check: текущая сигнатура `compute_verdict(op: dict, init: bool) -> str:` — параметра нет. T14 = verify + commit.

- [ ] **Step 1: Verify сигнатура**

```bash
grep -n "def compute_verdict" scripts/_apply_profile.py
```

Expected: `def compute_verdict(op: dict, init: bool) -> str:` (line 94).

- [ ] **Step 2: Verify все вызовы — два аргумента**

```bash
grep -n "compute_verdict(" scripts/_apply_profile.py
```

Expected: единственный вызов `compute_verdict(op_decl, init)` (line ~213).

- [ ] **Step 3: Commit (verify-only marker)**

```bash
git commit --allow-empty -m "$(cat <<'EOF'
chore(verify): compute_verdict signature без profile_dir [W4b-T14]

Reality-check spec'а: spec упоминает "drop profile_dir reserved param из
compute_verdict", но сигнатура уже compute_verdict(op, init) — параметра нет.
Закрыто на этапе W4a уже.

Verified:
- grep "def compute_verdict" → "def compute_verdict(op: dict, init: bool)"
- grep callers → "compute_verdict(op_decl, init)" (один вызов, 2 аргумента)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Phase P4 — Final smoke + lessons

### Task 15: Full smoke check

**Files:**
- Verify: `bash scripts/check.sh --full`

**Depends on:** all P1+P2+P3 tasks completed.

- [ ] **Step 1: Run full smoke**

```bash
bash scripts/check.sh --full 2>&1 | tee /tmp/w4b-final-smoke.log | tail -30
```

Expected: финальная строка `OK: <N> assertions pass` (или эквивалентная), без `FAIL`. Число assertions в диапазоне 170–195 (W4a baseline 176; T8 добавил 2 ассерта; T10/T12 добавили 2-4 ассерта; net примерно +5).

- [ ] **Step 2: Записать число ассертов**

```bash
grep -E "^OK|assertions" /tmp/w4b-final-smoke.log | tail -5
```

Запомнить число для lessons-learned (T16).

- [ ] **Step 3: Если есть FAIL — debug, не двигаться к T16**

Если ассертов меньше 170 или есть FAIL:
- Run `bash scripts/check.sh --full 2>&1 | grep -i fail`
- Fix — open отдельный fix-task (W4b-T15-fix).

- [ ] **Step 4: Если зелёный — commit**

```bash
git commit --allow-empty -m "$(cat <<'EOF'
chore(smoke): W4b full check.sh --full зелёный [W4b-T15]

Финальное число assertions: <N> (записано в lessons-learned T16).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

(Заменить `<N>` на реальное число.)

---

### Task 16: Lessons-learned запись

**Files:**
- Modify: `docs/lessons-learned.md`

**Depends on:** T15.

- [ ] **Step 1: Read текущий tail of lessons-learned**

```bash
tail -50 docs/lessons-learned.md
```

Найти место для новой записи (по convention — секция «Wave 4b» в конце).

- [ ] **Step 2: Append W4b retrospective**

Добавить в конец файла:

```markdown

## Wave 4b (2026-05-XX)

**Scope:** Content baseline minimization + W4a Medium cleanup. 16 atomic tasks.

**Что сделано:**
- Phase P1: baseline `content/` уменьшен с 11 файлов до 1 (`_index.md`); 5+6 op:delete сняты с kb-team/kb-product manifests; project content-scaffold обогащён `10-domain/research/_index.md` (мигрирован из baseline).
- Phase P2: 3 example каталога регенерированы; `test-template.sh` дополнен T-W4b-BASELINE assertions; validate-content.py проверен (нет хардкода baseline-имён).
- Phase P3: `_apply_profile.py` использует unified `ProfileError` (`apply_on_value_mutations`, `apply_dotted_mutation` больше не вызывают `sys.exit`). `apply-overlay.sh` оптимизирован — `ops_count` через `wc -l` вместо `python3 -c`. Dead field `role_status` удалён из 2 manifests + 6 test fixtures.

**Verify-only задачи (reality-check spec'а):**
- T9 — validate-content.py не зашит на baseline-имена.
- T12 — multiline reason strip уже сделан в W4a (line 207 `_apply_profile.py`).
- T14 — compute_verdict сигнатура уже `(op, init)` без `profile_dir`.

Эти задачи всё равно получили коммиты для трассировки и explicit assertions (T12 добавил unit-test).

**Метрики:**
- Финальный smoke: `bash scripts/check.sh --full` → <N> ассертов зелёные (W4a baseline 176, +<diff>).
- Manifest size: kb-team манифест уменьшен на 5 op-блоков (-15 строк); kb-product — на 6 (-18 строк).
- `apply-overlay.sh`: один `python3` invocation вместо двух в счастливом пути.

**Уроки:**
- Spec написанный по memory может содержать stale задачи (3 verify-only из 16). Reality-check после spec и до plan экономит subagent-время.
- Backwards-compat alias (`ManifestError = ProfileError`) — дешевый способ refactor'а с минимальным риском для callers.
- `examples/` регенерация диффом проверяет идемпотентность профилей лучше чем синтетические тесты.

**Follow-up'ы для W4c+:**
- 4 stub профиля → stable (`product`, `methodology`, `course`, `custom`).
- Init UX customization (interactive prompts).
- Migration tooling (`migrate-profile.sh`, `upgrade-template.sh`) для пользователей со старым baseline.
- Hardcoded `--base-dir` в `op_resolve_agents`.
```

(Заменить `<N>` и `<diff>` на реальные числа из T15.)

- [ ] **Step 3: Verify lessons-learned валиден**

```bash
python3 scripts/validate-content.py docs/ 2>/dev/null || true
# (validate-content.py обычно валидирует только Gramax content/, но если
# подставить docs/ — он проверит markdown структуру; ошибки игнорируем)
```

- [ ] **Step 4: Commit**

```bash
git add docs/lessons-learned.md
git commit -m "$(cat <<'EOF'
docs(lessons): W4b retrospective [W4b-T16]

Scope, метрики, verify-only reality-check уроки, follow-up'ы W4c+.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Self-Review

### Spec coverage

| Spec section | Plan tasks |
|--------------|-----------|
| §2.1 Baseline `content/` = только `_index.md` | T1 (migrate research), T2 (delete folders) |
| §2.2 Профильные манифесты — снять op:delete | T3 (kb-team), T4 (kb-product) |
| §2.3 W4a cleanup — ProfileError | T10 |
| §2.3 ops_count optimize | T11 |
| §2.3 multiline reason strip | T12 (verify-only) |
| §2.3 drop role_status | T13 |
| §2.3 drop profile_dir reserved | T14 (verify-only) |
| §2.4 Examples regenerate | T5/T6/T7 |
| §3 затронутые файлы | покрыты T1-T14 |
| §6 риск 4 (`role_status` validator references) | T13 step 1 (grep verify) |
| §7 testing strategy | unit-tests в T8/T10/T12; integration в T15 |

### Placeholder scan

- ✅ Нет `TBD/TODO`.
- ✅ Все code-блоки содержат полный контент.
- ✅ Все commit-сообщения готовы (с заглушками `<N>` для metrics в T15/T16, что явно отмечено).
- ⚠ T1 step 4 формулировка «(скрипт-заглушка — реально выполнить только для путей из Step 3)» — это инструкция для исполнителя, не placeholder; принимается.

### Type consistency

- `ProfileError` определён в T10; `ManifestError = ProfileError` alias сохраняется.
- `compute_verdict(op, init)` — двухаргументная сигнатура везде (T14 verify).
- Test prefix `T-W4b-T<N>` consistent в T8, T10, T12.

### Issues found / fixes inline

- Поправить spec-mention в T15 — диапазон assertions «170–195» соответствует §9 spec (там 170–195). ✓ Соответствует.
- T1 path inventory: spec §3 упоминает `10-domain/research/_index.md` как пример; план явно делает Step 3 audit для возможных других расхождений. ✓

---

## Execution Handoff

**Plan complete and saved to** `docs/superpowers/plans/2026-05-07-wave-4b.md`. **Two execution options:**

**1. Subagent-Driven (recommended)** — fresh subagent per task, two-stage review (spec compliance + code quality), fast iteration. Хорошо подходит для W4b: 16 atomic tasks, P3 параллельно P1/P2.

**2. Inline Execution** — выполнение в текущей сессии через `superpowers:executing-plans`, batch с checkpoints.

**Auto mode active** — рекомендую **Subagent-Driven**: P3 потоки запускаются параллельно P1, экономия wall-clock ≈ 30%.
